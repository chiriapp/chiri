# required for `op` to know which account to use
export OP_ACCOUNT=chiri.1password.com

if has nix; then
  use flake
fi

# Point git at our tracked hooks directory so hooks activate automatically.
git config core.hooksPath scripts/hooks
